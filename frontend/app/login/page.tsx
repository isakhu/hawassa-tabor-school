// Login Page
// This page renders the login form for all user roles (admin, teacher, student).
// It will handle form submission, call the auth API, store the JWT token,
// and redirect users to their respective dashboards based on their role.

export default function LoginPage() {
  return null;
}
